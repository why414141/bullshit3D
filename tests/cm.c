//camera movement
#include "../rdr/render.h"
#include "../rdr/mesh.h"
#include "../stl/stl.h"
#include "../rdr/render.h"
int main(){
	Mesh * mesh = readStl("cmMesh.stl", 0,0);
	Buffer * buffer = createBuffer(64,64);	
	Mesh * sc_mesh = createMesh();
	//in softwa:w:
}
